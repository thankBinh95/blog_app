<%= form_for(current_user.active_interactives.build) do |f| %>
  <div><%= hidden_field_tag :followed_id, @user.id %></div>
  <%= f.submit t("users.submit_follow"), class: "btn btn-primary" %>
<% end %>
